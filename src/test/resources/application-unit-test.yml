spring:
  main:
    allow-bean-definition-overriding: true

api:
  external:
    auth:
      base-url: http://localhost:${wiremock.server.port}
    users:
      base-url: http://localhost:${wiremock.server.port}
    notes:
      base-url: http://localhost:${wiremock.server.port}